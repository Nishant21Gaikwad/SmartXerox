# SmartXerox - System Architecture

## 🏗️ High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     SMARTXEROX SYSTEM                        │
└─────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐         ┌──────────────────┐
│                  │         │                  │         │                  │
│   STUDENT WEB    │◄───────►│  EXPRESS.JS API  │◄───────►│   SUPABASE DB    │
│   (React + Vite) │         │   (Node.js)      │         │   (PostgreSQL)   │
│                  │         │                  │         │                  │
└──────────────────┘         └──────────────────┘         └──────────────────┘
         │                            │                            │
         │                            │                            │
         │                            ▼                            │
         │                   ┌──────────────────┐                 │
         │                   │                  │                 │
         └──────────────────►│ SUPABASE STORAGE │◄────────────────┘
                             │   (File Bucket)  │
                             │                  │
                             └──────────────────┘
                                      ▲
                                      │
                                      │
                             ┌──────────────────┐
                             │   CRON JOB       │
                             │ (Auto Cleanup)   │
                             │  Runs Hourly     │
                             └──────────────────┘
```

---

## 🔄 Data Flow Diagrams

### Student Order Flow

```
┌─────────┐
│ Student │
└────┬────┘
     │
     │ 1. Fill form + upload file
     ▼
┌────────────────┐
│ React Frontend │
└────────┬───────┘
         │
         │ 2. POST /api/orders (FormData)
         ▼
┌────────────────┐
│ Express Server │──────► Validate file type/size
└────────┬───────┘
         │
         │ 3. Upload file
         ▼
┌────────────────┐
│ Supabase       │
│ Storage        │
└────────┬───────┘
         │
         │ 4. Return file URL
         ▼
┌────────────────┐
│ Express Server │
└────────┬───────┘
         │
         │ 5. Create order record
         ▼
┌────────────────┐
│ Supabase DB    │
└────────┬───────┘
         │
         │ 6. Return order details
         ▼
┌────────────────┐
│ React Frontend │──────► Display success + order ID
└────────────────┘
```

### Admin Update Flow

```
┌───────┐
│ Admin │
└───┬───┘
    │
    │ 1. Login
    ▼
┌────────────────┐
│ Admin Login    │
└────────┬───────┘
         │
         │ 2. POST /api/admin/login
         ▼
┌────────────────┐
│ Express Server │──────► Verify credentials
└────────┬───────┘
         │
         │ 3. Generate JWT token
         ▼
┌────────────────┐
│ Admin Dashboard│──────► Store token in localStorage
└────────┬───────┘
         │
         │ 4. GET /api/admin/orders (with token)
         ▼
┌────────────────┐
│ Express Server │──────► Verify JWT
└────────┬───────┘
         │
         │ 5. Fetch orders
         ▼
┌────────────────┐
│ Supabase DB    │
└────────┬───────┘
         │
         │ 6. Return orders
         ▼
┌────────────────┐
│ Admin Dashboard│──────► Display orders table
└────────┬───────┘
         │
         │ 7. Update status dropdown
         ▼
┌────────────────┐
│ Express Server │
└────────┬───────┘
         │
         │ 8. PUT /api/admin/orders/:id/status
         ▼
┌────────────────┐
│ Supabase DB    │──────► Update order status
└────────────────┘
```

### Auto-Cleanup Flow

```
⏰ Every Hour
     │
     ▼
┌────────────────┐
│  Cron Job      │
│  (Node-Cron)   │
└────────┬───────┘
         │
         │ 1. Calculate 24h ago timestamp
         ▼
┌────────────────┐
│ Supabase DB    │
└────────┬───────┘
         │
         │ 2. Query orders older than 24h
         ▼
┌────────────────┐
│  Expired       │
│  Orders List   │
└────────┬───────┘
         │
         ├─────► 3a. Delete files from Storage
         │       ┌────────────────┐
         │       │ Supabase       │
         │       │ Storage        │
         │       └────────────────┘
         │
         └─────► 3b. Delete order records
                 ┌────────────────┐
                 │ Supabase DB    │
                 └────────────────┘
                 
✅ Cleanup Complete
```

---

## 📦 Component Hierarchy

### Frontend (React)

```
App
├── Router
│   ├── / (Home)
│   │   └── StudentPanel
│   │       ├── Navbar
│   │       ├── Upload Form
│   │       │   └── File Input
│   │       └── Order Tracking
│   │           └── OrderCard[]
│   │               └── StatusBadge
│   │
│   ├── /admin/login
│   │   └── AdminLogin
│   │       └── Login Form
│   │
│   └── /admin/dashboard
│       └── AdminDashboard
│           ├── Navbar
│           ├── Statistics Cards
│           ├── Filter Controls
│           └── Orders Table
│               └── Status Dropdown[]
```

### Backend (Express)

```
server.js
├── Middleware
│   ├── cors()
│   ├── express.json()
│   └── express.urlencoded()
│
├── Routes
│   ├── /api/orders
│   │   ├── POST   / (create order + upload)
│   │   ├── GET    /:phoneNumber (track orders)
│   │   └── DELETE /:id (delete order)
│   │
│   └── /api/admin
│       ├── POST /login (authenticate)
│       ├── GET  /orders (all orders) [Protected]
│       ├── PUT  /orders/:id/status [Protected]
│       └── GET  /stats [Protected]
│
├── Middleware
│   ├── authMiddleware (verify JWT)
│   └── isAdmin (check admin role)
│
└── Cron Jobs
    └── cleanup (hourly)
```

---

## 🗄️ Database Schema

```
┌─────────────────────────────────────────────────────────┐
│                    orders                                │
├─────────────────────────────────────────────────────────┤
│ id               UUID          PRIMARY KEY               │
│ student_name     TEXT          NOT NULL                  │
│ phone_number     TEXT          NOT NULL [INDEXED]        │
│ file_url         TEXT          NOT NULL                  │
│ file_path        TEXT          NOT NULL                  │
│ copies           INTEGER       NOT NULL                  │
│ color_type       TEXT          CHECK ('B&W', 'Color')    │
│ status           TEXT          CHECK (enum) [INDEXED]    │
│ created_at       TIMESTAMP     DEFAULT NOW() [INDEXED]   │
└─────────────────────────────────────────────────────────┘

Indexes:
- idx_orders_phone_number (for quick student lookups)
- idx_orders_created_at (for cleanup queries)
- idx_orders_status (for filtering)
```

---

## 🔐 Security Layers

```
┌──────────────────────────────────────────────┐
│           Request Security Flow               │
└──────────────────────────────────────────────┘

Student Request:
┌─────────────┐
│ HTTP Request│
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ CORS Validation │  ◄── Check origin
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ File Validation │  ◄── Type, size checks
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Input Validation│  ◄── Required fields
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Process Request │
└─────────────────┘


Admin Request:
┌─────────────┐
│ HTTP Request│
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ CORS Validation │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ JWT Verification│  ◄── authMiddleware
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Role Check      │  ◄── isAdmin
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Process Request │
└─────────────────┘
```

---

## 🌐 Deployment Architecture

```
┌────────────────────────────────────────────────────────┐
│                   PRODUCTION SETUP                      │
└────────────────────────────────────────────────────────┘

        ┌──────────────┐
        │    GitHub    │
        │  Repository  │
        └───────┬──────┘
                │
        ┌───────┴────────┐
        │                │
        ▼                ▼
┌──────────────┐  ┌──────────────┐
│   Vercel     │  │   Railway    │
│  (Frontend)  │  │  (Backend)   │
└──────┬───────┘  └──────┬───────┘
       │                 │
       │                 │
       │  API Calls      │
       └────────┬────────┘
                │
                ▼
        ┌──────────────┐
        │   Supabase   │
        │ (Database +  │
        │   Storage)   │
        └──────────────┘

Environment Variables:
- Vercel: VITE_API_URL → Railway URL
- Railway: All backend secrets
- Supabase: Managed in dashboard
```

---

## 📊 Status State Machine

```
Order Status Flow:

    ┌──────────┐
    │ Created  │
    └────┬─────┘
         │
         ▼
    ┌──────────┐
    │ In Queue │ ◄─────┐
    └────┬─────┘       │
         │             │
         ▼             │
    ┌──────────┐      │
    │ Printing │ ─────┘ (can go back)
    └────┬─────┘
         │
         ▼
    ┌──────────┐
    │  Ready   │
    └────┬─────┘
         │
         ▼
    ┌──────────┐
    │Delivered │
    └────┬─────┘
         │
         ▼
    ┌──────────┐
    │ Expired  │ ◄── After 24h (Auto-deleted)
    └──────────┘
```

---

## 🔄 File Lifecycle

```
Upload → Store → Access → Delete

1. UPLOAD
   Student uploads → Express receives → Multer processes
   
2. STORE
   Supabase Storage saves → Returns public URL
   
3. ACCESS
   Student views → Admin downloads → Direct S3-like access
   
4. DELETE
   After 24h → Cron job → Supabase Storage removes
   OR
   Student deletes → Immediate removal
```

---

## ⚡ Performance Optimizations

```
Frontend:
- Vite for fast builds
- Code splitting (lazy loading ready)
- Tailwind CSS (purged in production)
- Axios for efficient HTTP

Backend:
- Database indexes on frequently queried fields
- Multer memory storage (fast processing)
- Direct Supabase storage (no local filesystem)
- JWT for stateless authentication

Database:
- Indexed: phone_number, created_at, status
- UUID for distributed systems
- Timestamp with timezone
```

---

## 📈 Scalability Considerations

```
Current Setup (MVP):
- Single admin user
- Single location
- In-memory multer
- Hourly cleanup

Future Scaling:
1. Multiple Admins
   - Add users table
   - Role-based access control
   
2. Multiple Locations
   - Add location_id to orders
   - Multi-tenant architecture
   
3. High Volume
   - Add Redis caching
   - Queue system (Bull/RabbitMQ)
   - Load balancer
   
4. Advanced Features
   - CDN for file delivery
   - Real-time notifications (WebSockets)
   - Analytics service
```

---

This architecture supports the entire SmartXerox system and is designed for easy extension and scaling! 🚀
